<h1>タスク作成</h1>
<%= form_with model: @task, local: false do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.submit%>
<% end %>